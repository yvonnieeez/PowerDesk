const ROOMS = ['drawing-room', 'work-room-1', 'work-room-2'];

const FAN_POWER_WATTS = 60;
const LIGHT_POWER_WATTS = 15;

const DEVICES_PER_ROOM = {
  fans: 2,
  lights: 3,
};

module.exports = {
  ROOMS,
  FAN_POWER_WATTS,
  LIGHT_POWER_WATTS,
  DEVICES_PER_ROOM,
};
